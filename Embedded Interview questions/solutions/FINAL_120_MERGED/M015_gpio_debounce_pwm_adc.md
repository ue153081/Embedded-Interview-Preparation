# M015 — GPIO, debounce, PWM, ADC & sensor wrapper

**Type:** Coding (C)  
**Merged from:** Q055, Q056, Q057, Q058  
**Companies:** Apple · Tesla  
**Tier:** S (must-know)

**Question:** GPIO read/write/toggle + interrupt debounce; PWM period/duty; ADC single-shot; thin sensor driver on I2C.

---

## Sub-variant coverage

| Original Q | Sub-variant |
|---|---|
| Q055 | volatile ADC pitfalls |
| Q056 | Thermal read retry |
| Q057 | Symmetric debounce |
| Q058 | See merged solution |

---

## Step 0 — Clarifying questions (say these out loud)

- **Candidate:** Debounce in ISR or timer callback?
- **Candidate:** ADC blocking with timeout?

## Step 1 — Approach

Build GPIO, debounce, PWM, ADC & sensor wrapper in layers: invariants first, happy path, then edge cases and concurrency.

- Restate API signatures and invariants aloud before coding.
- Implement core logic with straightforward loops; optimize after tests pass.
- Document ownership, error codes, and ISR vs task context.

## Step 2 — Data structures / invariants

1. Structs/enums matching API — invariants in comments.
2. Platform hooks (`irq_save`, `now_ms`) isolated for host test fakes.

## Step 3 — Complete solution (compilable C)

```c
#include <stdint.h>
#include <stddef.h>

typedef void (*debounce_cb)(int level, void *ctx);

typedef struct {
    uint32_t delay_ms;
    debounce_cb cb;
    void *ctx;
    uint8_t pending_level;
    uint8_t armed;
} debounce_t;

static uint32_t g_now;

extern void sw_timer_set(void *t, uint32_t ms, void (*cb)(void*), void *ctx);
extern void sw_timer_cancel(void *t);

static void debounce_fire(void *ctx) {
    debounce_t *d = (debounce_t *)ctx;
    d->armed = 0;
    if (d->cb) {
        d->cb((int)d->pending_level, d->ctx);
    }
}

void debounce_on_edge(debounce_t *d, int level) {
    d->pending_level = (uint8_t)level;
    sw_timer_cancel(&d->armed);
    sw_timer_set(&d->armed, d->delay_ms, debounce_fire, d);
}

/* --- next section --- */

#include <stdint.h>
#include <stddef.h>
int exti_enable(unsigned line){(void)line;return 0;}
```

## Step 4 — Complexity

| Operation | Time | Space |
|---|---:|---:|
| primary API | O(1) typical | O(1) or O(cap) |

## Step 5 — Edge cases

1. NULL / zero-length — defined error or no-op.
2. Boundary at max capacity — no overrun.
3. Repeated calls idempotent where API requires.

## Step 6 — Concurrency / ISR / context notes

Label ISR-writable vs task-only fields. Keep ISR push O(1); defer parsing to task.

## Step 7 — Follow-up answers

**Q: Debounce approach?**  
**A:** Restart timer on each edge; fire after quiet period — separate press/release if needed.


## Step 8 — Tests

1. Happy path — minimal valid input produces expected output.
2. Zero/null/empty — defined error, no crash.
3. Boundary — max capacity or timeout edge.
4. Stress — back-to-back calls or burst traffic.

## Further study

- [GPIO Configuration](https://github.com/theEmbeddedGeorge/theEmbeddedNewTestament.github.io/blob/master/Hardware_Fundamentals/GPIO_Configuration.md)
- [Analog I/O](https://github.com/theEmbeddedGeorge/theEmbeddedNewTestament.github.io/blob/master/Hardware_Fundamentals/Analog_I_O.md)

---

*Generated by `tools/generate_merged_solutions.py` for M015.*