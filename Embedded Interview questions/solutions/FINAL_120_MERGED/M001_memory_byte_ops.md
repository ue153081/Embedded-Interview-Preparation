# M001 — Memory byte operations (memcpy / memmove / memset / secure wipe)

**Type:** Coding (C)  
**Merged from:** Q001, Q002  
**Companies:** Google · Apple · NVIDIA · Tesla  
**Tier:** S (must-know)

**Question:** Implement overlap-safe memmove, optimized memcpy (alignment paths), and memset; explain secure zeroization.

---

## Sub-variant coverage

| Original Q | Sub-variant |
|---|---|
| Q001 | NULL / zero-length semantics |
| Q002 | Overlapping regions (memmove direction) |
| — | Word-aligned fast path vs byte tail |
| — | volatile/memset_s for secrets |

---

## Step 0 — Clarifying questions (say these out loud)

- **Candidate:** May source and destination overlap? I'll implement memmove with direction choice.
- **Candidate:** Alignment assumptions — I'll add word-aligned fast path when both pointers align.
- **Candidate:** For secrets, should I use volatile wipe or memset_s-style API?

## Step 1 — Approach

Build Memory byte operations (memcpy / memmove / memset / secure wipe) in layers: invariants first, happy path, then edge cases and concurrency.

- Restate API signatures and invariants aloud before coding.
- Implement core logic with straightforward loops; optimize after tests pass.
- Document ownership, error codes, and ISR vs task context.

## Step 2 — Data structures / invariants

1. Structs/enums matching API — invariants in comments.
2. Platform hooks (`irq_save`, `now_ms`) isolated for host test fakes.

## Step 3 — Complete solution (compilable C)

```c
#include <stdint.h>
#include <stddef.h>
static void *copy_bytes(void *dst, const void *src, size_t n) {
    unsigned char *d = (unsigned char *)dst;
    const unsigned char *s = (const unsigned char *)src;
    for (size_t i = 0;
    i < n;
    ++i) {
        d[i] = s[i];
    }
    return dst;
}
void *my_memcpy(void *dst, const void *src, size_t n) {
    if (n == 0) {
        return dst;
    }
    uintptr_t da = (uintptr_t)dst;
    uintptr_t sa = (uintptr_t)src;
    if ((da & 3u) == 0 && (sa & 3u) == 0) {
        while (n >= 16) {
            ((uint32_t *)da)[0] = ((const uint32_t *)sa)[0];
            ((uint32_t *)da)[1] = ((const uint32_t *)sa)[1];
            ((uint32_t *)da)[2] = ((const uint32_t *)sa)[2];
            ((uint32_t *)da)[3] = ((const uint32_t *)sa)[3];
            da += 16;
            sa += 16;
            n -= 16;
        }
        while (n >= 4) {
            *(uint32_t *)da = *(const uint32_t *)sa;
            da += 4;
            sa += 4;
            n -= 4;
        }
    }
    return copy_bytes((void *)da, (const void *)sa, n);
}
/* --- next section --- */
#include <stdint.h>
#include <stddef.h>
void *my_memmove(void *dst, const void *src, size_t n) {
    unsigned char *d = (unsigned char *)dst;
    const unsigned char *s = (const unsigned char *)src;
    if (n == 0 || dst == src) {
        return dst;
    }
    if (d < s) {
        for (size_t i = 0;
        i < n;
        ++i) {
            d[i] = s[i];
        }
    }
    else {
        for (size_t i = n;
        i > 0;
        --i) {
            d[i - 1] = s[i - 1];
        }
    }
    return dst;
}
/* --- next section --- */
#include <stdint.h>
#include <stddef.h>
#include <string.h>
void *my_memset(void *s,int c,size_t n) {
    unsigned char *p=s;
    while(n--) *p++=(unsigned char)c;
    return s;
}
void secure_zero(void *s,size_t n) {
    volatile unsigned char *p=s;
    while(n--) *p++=0;
}
```

## Step 4 — Complexity

| Operation | Time | Space |
|---|---:|---:|
| primary API | O(1) typical | O(1) or O(cap) |

## Step 5 — Edge cases

1. NULL / zero-length — defined error or no-op.
2. Boundary at max capacity — no overrun.
3. Repeated calls idempotent where API requires.

## Step 6 — Concurrency / ISR / context notes

Label ISR-writable vs task-only fields. Keep ISR push O(1); defer parsing to task.

## Step 7 — Follow-up answers

**Q: Why is memcpy UB on overlap?**  
**A:** memcpy assumes non-overlapping regions; memmove handles forward/backward copy based on address order.

**Q: When does the compiler elide memset?**  
**A:** When it proves the buffer is dead before any secret use; for crypto keys use explicit_bzero or volatile loop.


## Step 8 — Tests

1. Happy path — minimal valid input produces expected output.
2. Zero/null/empty — defined error, no crash.
3. Boundary — max capacity or timeout edge.
4. Stress — back-to-back calls or burst traffic.

## Further study

- [Memory Management](https://github.com/theEmbeddedGeorge/theEmbeddedNewTestament.github.io/blob/master/Embedded_C/Memory_Management.md)
- [Structure Alignment](https://github.com/theEmbeddedGeorge/theEmbeddedNewTestament.github.io/blob/master/Embedded_C/Structure_Alignment.md)

---

*Generated by `tools/generate_merged_solutions.py` for M001.*